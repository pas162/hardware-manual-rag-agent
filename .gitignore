# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/

# Environment
.env
*.env

# Data — generated artifacts, PDFs, and local stores (not source)
data/pdfs/
data/parsed/
data/figures/
data/store/

# Keep the registry template (it is source, not a generated artifact)
!data/registry.json

# IDE / OS
.vscode/
.DS_Store
Thumbs.db

# AI assistant local config
.claude/
.rica/

# MCP local config (contains machine-specific paths)
.mcp.json

# Docs (internal planning notes)
docs/

# POC report
poc_report.html

# Eval outputs — golden_set_v2 is dev-only (RA6M4-specific)
eval/results.md
eval/golden_set_v2.csv
eval_output/
